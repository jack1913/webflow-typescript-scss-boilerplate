/* eslint-disable no-undef */
/**
 @type {import('./typings/config.types').Config}
 */

module.exports = {
  // If specified will open on npm:serve a web browser
  // hostname: 'https://your-site.webflow.io',
  // Port from which webpack will be served in localhost
  port: 8080,
  pages: {
    home: './src/home.ts',
  },
};
