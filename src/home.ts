require('./home.scss');

// Check if HMR interface is enabled
// DON'T DELETE if you want to use HMR while developing.
if (module.hot) {
  module.hot.accept();
}

console.log('Test message from hello.ts - Hello?');
